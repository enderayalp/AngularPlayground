import { Routes } from '@angular/router';
import { ObservableOverview } from './observable-overview/observable-overview';

export const routes: Routes = [{ path: '', component: ObservableOverview, pathMatch: 'full' }];
